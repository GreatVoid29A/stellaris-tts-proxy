# Stellaris TTS Replacement Configuration
# Place this file in the same directory as Stellaris.exe

# ==================== SERVER SETTINGS ====================

# OpenAI-compatible TTS server URL (without trailing slash)
server=https://api.openai.com/v1

# TTS model to use
# Options: tts-1, tts-1-hd, gpt-4o-mini-tts
# Recommended for OpenAI: gpt-4o-mini-tts
model=gpt-4o-mini-tts

# Voice to use
# Options: alloy, ash, bald, coral, echo, fable, nova, onyx, sage, shimmer, verse, marin, cedar
# Recommended for best quality: marin, cedar
voice=marin

# API key (leave empty if not required, or use dummy key)
api_key=sk-your-actual-api-key-here

# ==================== AUDIO SETTINGS ====================

# Audio format: wav, mp3, opus, aac, flac
format=mp3

# Volume level (0-100)
volume=100

# ==================== CACHE SETTINGS ====================

# Maximum number of audio files to keep in memory cache
# Default: 50
max_cache_size=50

# Enable disk cache (1 = enabled, 0 = disabled)
# When enabled, cached audio is saved to cache/ subdirectory
enable_disk_cache=1

# Maximum disk cache size in megabytes
# Default: 1000
max_disk_cache_mb=1000

# ==================== PARALLEL FETCHING ====================

# Maximum number of parallel fetch threads
# Default: 4
max_fetch_threads=4

# Maximum number of pending fetch requests in queue
# Default: 20
max_pending_fetches=20

# ==================== GAME SETTINGS ====================

# Mute original game TTS (1 = mute, 0 = play both)
mute_original=1

# Hotkey to cancel current audio playback
# Options: F1-F12, ESC, SPACE, ENTER, TAB, A-Z, 0-9
# Default: F9
cancel_key=F9

# ==================== LOGGING SETTINGS ====================

# Log level: debug, info, warning, error
log_level=info

# Show console window (1 = show, 0 = hide)
show_console=1

# Enable file logging to tts_proxy.log (1 = enabled, 0 = disabled)
log_to_file=1
